# Database Replica Lag Response

Use this source when reporting data is stale because a read replica is lagging. Check replication delay, long-running transactions, disk throughput, network latency, and recent maintenance. Route to Database Operations to restart replication only after confirming no data corruption indicators and no active failover event.

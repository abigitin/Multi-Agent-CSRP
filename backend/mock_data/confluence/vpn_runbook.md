# VPN Client Connectivity Runbook

If authentication succeeds but internal dashboards time out after a VPN update, validate split-tunnel routes, flush DNS, and confirm the gateway profile includes dashboard subnets. Ask the user to reconnect after policy refresh.


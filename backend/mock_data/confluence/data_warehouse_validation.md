# Data Warehouse Validation Stall

Use this source when the nightly warehouse load is stuck at validation. Review validation task logs, source extract completeness, schema drift, rejected row counts, and dependency status. Data Engineering should rerun validation after correcting malformed records or schema mismatches. Notify report owners if refresh SLA is at risk.
